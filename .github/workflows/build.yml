name: Build

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        nimversion:
          - binary:stable

        os: [ubuntu-latest, macos-latest, windows-latest]
        include:
          - os: ubuntu-latest
            filename: mcsrvstat
            artifact: linux-x64

          - os: macos-latest
            filename: mcsrvstat
            artifact: darwin-x64

          - os: windows-latest
            filename: mcsrvstat.exe
            artifact: win-x64

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Setup Nim
      uses: iffy/install-nim@v4
      with:
        version: ${{ matrix.nimversion }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Check package validity
      run: nimble check

    - name: Download DLLs
      uses: valitydev/action-download-file@v1
      with:
        url: https://nim-lang.org/download/dlls.zip
        target-path: .

    - name: Unzip
      uses: montudor/action-zip@v1
      with:
        args: unzip -qq dlls.zip

    - name: Add necessary DLLs
      run: openssl s_client -CAfile cacert.pem

    - name: Build
      run: nimble build --define:release --accept

    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: ${{ matrix.artifact }}
        path: ${{ matrix.filename }}